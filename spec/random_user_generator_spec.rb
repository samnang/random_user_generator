require 'spec_helper'

describe RandomUserGenerator do
  it 'has a version number' do
    expect(RandomUserGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
